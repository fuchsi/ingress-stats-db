#jQuery ->
#  $('#screenshot').attr('name','screenshot')
#  $('#new_import').fileupload
#    dataType: 'script'
#    url: '<%= ::Rails.application.routes.url_helpers.import_upload_url(only_path: true) %>'
#    add: (e, data) ->
#      types = /(\.|\/)(gif|jpe?g|png)$/i
#      file = data.files[0]
#      if types.test(file.type) || types.test(file.name)
#        data.context = $(tmpl("template-upload", file))
#        $('#screenshot-container').append(data.context)
#        data.submit()
#      else
#        alert("#{file.name} is not a gif, jpg or png image file")
#    progress: (e, data) ->
#      if data.context
#        progress = parseInt(data.loaded / data.total * 100, 10)
#        data.context.find('.bar').css('width', progress + '%')
#  $('#new_import').bind 'fileuploadsubmit', (e, data) ->
#    input = $('#agent_stat_id')
#    data.formData = {id: input.val()}
#
